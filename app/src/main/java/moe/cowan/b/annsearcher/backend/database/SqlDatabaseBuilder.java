package moe.cowan.b.annsearcher.backend.database;

import android.app.Application;

/**
 * Created by user on 04/07/2015.
 */
public class SqlDatabaseBuilder {

    public static DatabaseProxy createDatabase(String databaseName, Application parentApplication) {
        /*InternalDatabase internalDatabase = null;
        internalDatabase = new SqliteInternalDatabase(DATABASE_NAME, getApplication());
        DatabaseDataProxy dataProxy;
        DatabaseCache<Document> cache = new AnnDatabaseCache<>();
        dataProxy = new AnnDatabaseProxy(cache);
        DatabaseListProxy dbListProxy = new MalDatabaseProxy();
        InternalDatabaseProxy internalDatabaseProxy = new InternalDatabaseProxy(internalDatabase, dataProxy, dbListProxy);*/
        return null;
    }

}
